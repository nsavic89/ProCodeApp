import React from 'react';

function Dictionary(props) {

    if (props.visible === false) {
        return <div />
    }

    return(
        <div>
            test
        </div>
    )
}
export default Dictionary;